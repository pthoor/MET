assessments/
output/
staging/
.claude/
superpowers/
# Local-only working files (connection helpers, audit notes). Never committed:
# this repo is public and these may reference unpatched security findings.
*.local.ps1
*.local.md

# Pester and coverage output
TestResults*.xml
coverage.xml

# NuGet packages from local Publish-Module testing
*.nupkg

# Certificates / keys (never commit private key material)
*.pfx
*.key
*.pem
*.cer
*.crt

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.log
